# Hegelion Environment Configuration
# Copy this file to .env and fill in your API keys

# ============================================================================
# Provider Configuration
# ============================================================================
# Choose your LLM provider: anthropic, openai, google, ollama, custom_http, or auto
# Default: anthropic
HEGELION_PROVIDER=anthropic

# Model identifier (varies by provider)
# Default: claude-4.5-sonnet-latest
HEGELION_MODEL=claude-4.5-sonnet-latest

# ============================================================================
# Anthropic / Claude Configuration (default)
# ============================================================================
# Required for Anthropic provider
ANTHROPIC_API_KEY=your-anthropic-api-key-here

# Optional: Override API base URL (default: https://api.anthropic.com)
# ANTHROPIC_BASE_URL=https://api.anthropic.com

# ============================================================================
# OpenAI Configuration
# ============================================================================
# Required for OpenAI provider
# OPENAI_API_KEY=your-openai-api-key-here

# Optional: Override API base URL (default: https://api.openai.com/v1)
# OPENAI_BASE_URL=https://api.openai.com/v1

# Optional: Organization ID for OpenAI
# OPENAI_ORG_ID=your-org-id

# ============================================================================
# Google Gemini Configuration
# ============================================================================
# Required for Google provider
# GOOGLE_API_KEY=your-google-api-key-here

# Optional: Override API base URL (default uses google-generativeai defaults)
# GOOGLE_API_BASE_URL=https://generativelanguage.googleapis.com

# ============================================================================
# Ollama Configuration (local)
# ============================================================================
# For Ollama provider, set HEGELION_PROVIDER=ollama
# Optional: Override Ollama base URL (default: http://localhost:11434)
# OLLAMA_BASE_URL=http://localhost:11434

# ============================================================================
# Custom HTTP Backend Configuration
# ============================================================================
# For custom HTTP provider, set HEGELION_PROVIDER=custom_http
# Required: Base URL for your custom API endpoint
# CUSTOM_API_BASE_URL=https://your-endpoint.example.com/v1/run

# Optional: API key for custom backend
# CUSTOM_API_KEY=your-custom-api-key

# Optional: Request timeout in seconds (default: 60.0)
# CUSTOM_API_TIMEOUT=60.0

# ============================================================================
# Engine Settings (optional)
# ============================================================================
# Synthesis threshold (0.0-1.0) - controls when synthesis is triggered
# Default: 0.85
# HEGELION_SYNTHESIS_THRESHOLD=0.85

# Maximum tokens per phase (thesis, antithesis, synthesis)
# Default: 10000
# HEGELION_MAX_TOKENS_PER_PHASE=10000
